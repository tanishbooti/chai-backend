module.exports = {
    // Use double quotes instead of single
    singleQuote: false,
  
    // Put spaces between brackets: { foo: bar }
    bracketSpacing: true,
  
    // Number of spaces per indentation level
    tabWidth: 2,
  
    // Add trailing commas where valid in ES5 (like objects, arrays, etc.)
    trailingComma: 'es5',
  
    // Add semicolons at the end of statements
    semi: true,
  };
// Prettier is an opinionated code formatter that helps you enforce consistent code styles across your project. It automatically formats your code according to a predefined set of rules, 
//removing the need for manual formatting.
// npm i prettier